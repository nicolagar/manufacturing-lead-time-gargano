services:
  - type: web
    name: nested-pert-fullstack
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend
      npm install
      npm run build
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
